node_modules/
web/dist/
# runtime-config.js is committed as an empty stub so Netlify deploy includes it (gitignored
# artifacts can be omitted from the publish bundle). `npm run build` overwrites from .env / CI env.
.env
.secrets/
*.swp
.env.local
.DS_Store
logs/

# Agent/editor metadata (local only)
.agents/
.claude/
.cursor/
.skills/
skills/
skills-lock.json

# AI packing artifacts (can contain secrets)
repomix-output.xml
