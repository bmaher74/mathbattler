{
  "name": "mathbattler",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss -i ./tailwind.input.css -o ./tailwind.css --minify",
    "validate-llm": "node scripts/validate-llm.mjs",
    "serve": "node scripts/serve-static.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
